FEED THE DNA — IMAGE DROP FOLDER
================================

Drop your banner images here using these EXACT filenames (lowercase).
The website is already wired to use them. Until the files exist, the site
falls back to a matching cosmic gradient, so nothing looks broken.

REQUIRED FILES
--------------
1) empower-humanity.png  (in place ✓)
   The purple "Empower Humanity" cosmic / Flower-of-Life banner.
   Used as the full-screen HOMEPAGE hero background (index.html).
   Recommended: ~1600x1024 or larger, landscape, JPG (or PNG).

2) dnactivate.png  (in place ✓)
   The blue/orange "DNActivate" DNA + media banner.
   Used as the full-screen hero background on the DNActivate page (dnactivate.html).
   Recommended: ~1600x1024 or larger, landscape, JPG (or PNG).

OPTIONAL (nice to have later)
-----------------------------
3) founder.jpg        -> About page founder photo (replaces the "JS" placeholder)
4) og-image.jpg       -> social share preview image (1200x630)

NOTES
-----
- Keep the same filenames and the site picks them up automatically — no code changes needed.
- If you prefer PNG, rename to .png AND tell me so I can update the two references.
- Compress large photos (e.g. https://squoosh.app) so pages load fast.
- Aspect ratio: a wide landscape (~16:10 or 3:2) works best. The CENTER of the
  banner (wordmark + light burst) is always kept; the far left/right edges may be
  cropped on phones, so keep the important art near the middle. On very narrow
  phones the whole banner letterboxes onto the cosmic background instead of cropping.
